---
# Service del Inventory Service - ClusterIP (interno)
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: ecommerce
  labels:
    app.kubernetes.io/name: inventory-service
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app.kubernetes.io/name: inventory-service
  sessionAffinity: None

---
# Headless service para descubrimiento de pods
apiVersion: v1
kind: Service
metadata:
  name: inventory-service-headless
  namespace: ecommerce
  labels:
    app.kubernetes.io/name: inventory-service
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP
  selector:
    app.kubernetes.io/name: inventory-service
