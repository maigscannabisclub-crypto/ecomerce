---
# Service del Reporting Service - ClusterIP (interno)
apiVersion: v1
kind: Service
metadata:
  name: reporting-service
  namespace: ecommerce
  labels:
    app.kubernetes.io/name: reporting-service
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8086
      targetPort: 8086
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app.kubernetes.io/name: reporting-service
  sessionAffinity: None

---
# Headless service para descubrimiento de pods
apiVersion: v1
kind: Service
metadata:
  name: reporting-service-headless
  namespace: ecommerce
  labels:
    app.kubernetes.io/name: reporting-service
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 8086
      targetPort: 8086
      protocol: TCP
  selector:
    app.kubernetes.io/name: reporting-service
